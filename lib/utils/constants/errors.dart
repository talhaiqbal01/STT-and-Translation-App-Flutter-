/// This class contains all the App Errors in [String] formats.

class TErrors {
  TErrors._(); //-- To avoid creating instances

  // -- GLOBAL Texts
  static const String noConnection = 'noConnection';
  static const String unknownError = 'unknownError';

  // -- Translation Error Texts
  static const String translationError = 'translationError';
}
